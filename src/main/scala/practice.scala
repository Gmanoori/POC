object Practice extends App {
  def fibonacci(n:Int):Int ={
    def fiboTailRec(i:Int, last:Int, secondLast:Int):Int={
        i
    }
    n
  }
  def sampleDefArg(a:Int, b:Int = 2):Int ={
    a+b
  }
  println(sampleDefArg(1,4))

  val sampleStr = "hello Everyone 123 abc"
  print(sampleStr.length)
}